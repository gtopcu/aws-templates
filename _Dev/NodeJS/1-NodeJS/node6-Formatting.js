
var count = 2.222222;
console.log('count: %d', count);

const bucket = "arn:aws:s3::mybucket"
const message = `Bucket name is: ${bucket}`;
console.log(message);

const apiURLWithParams = `${parsedURL.url}?${stringifiedParams}`; //>ES6
