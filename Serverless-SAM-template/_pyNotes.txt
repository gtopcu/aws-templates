
python3 -m venv venv
source venv/bin/activate
pip install boto3
pip install aws_lambda_powertools
pip install python-dotenv (for .env files to work)
pip freeze
pip freeze > requirements.txt
pip install -r requirements.txt
deactivate

pytest --cov (include code coverage) -v(verbose) -s(prints outputs)
pytest::test_function

pip install poetry
poetry init (creates pyproject.toml + poetry.lock)
poetry config virtualenvs.in-project true
poetry install (also creates venv)
poetry env info -p
poetry env list
sudo poetry shell / exit / deactivate
poetry add requests
poetry remove requests